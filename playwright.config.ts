import { defineConfig } from "@playwright/test";

export default defineConfig({
  // Customize Playwright configuration here if needed
  // Example:
  // timeout: 60000,
  // use: {
  //   baseURL: "http://localhost:8080",
  // },
});
